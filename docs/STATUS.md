# STATUS (현재 개발 상태)

이 문서는 생기부 개인정보 익명화 도구 프로젝트의 **현재 구현 상태**, **다음 작업 계획**, **알려진 이슈** 및 **기술 부채**를 관리합니다.

---

## 1. 현재 구현율 및 진척 상황

* **현재 상태**: **v4.0 배포 준비 완료 (시니어 리뷰 피드백 최종 보완 완료)**
* **전체 진척률**: **100%**
* **개발 현황**:
  * 뼈대 구축(1단계) ➡️ 엔진 및 비동기 구현(2단계) ➡️ QSS 디자인 및 오동작 방지 폼 락(3단계) ➡️ 한글 경로 대응 및 PyInstaller 빌드 검증(4단계)을 마쳤으며, 시니어 리뷰어 피드백(v4.0)을 적극 반영하여 안정성 및 UI/UX 편의성 향상을 완료하였습니다.
  * 반영 완료 항목: UUID 기반 O(1) 데이터 바인딩 구조 구축, Span Overlap 알고리즘을 통한 중복 부분 문자열 탐지 차단, 비동기 QThread 취소 및 메인 윈도우 closeEvent 가드, 파일명 중복 순차 번호 부여 저장.

---

## 2. 다음 작업 계획 (Next Tasks)

* [ ] 실사용자(학교 업무 담당 교사) 환경 배포 및 피드백 수집.
* [ ] 대용량 엑셀 대장 탐색 및 매핑 연산 성능 모니터링.

---

## 3. 알려진 이슈 (Known Issues)

* **현재 발견된 활성 버그는 없습니다.**
* 탐지 방식의 한계 사항으로 존재했던 중복 부분 문자열 검출 오탐은 Span Overlap 충돌 차단 알고리즘을 도입하여 해결되었습니다.

---

## 4. Technical Debt (기술 부채)

1. **AppState 직접 수정 구조**
   * 현재 `AppController` 내에서 `self.state.selected_files = ...` 형식으로 상태 필드를 직접 덮어쓰고 있습니다. 프로젝트 규모가 확장될 경우 상태 추적이 어려울 수 있으므로, 상태 변경용 액션 패턴이나 캡슐화 메서드를 도입하는 개선이 필요할 수 있습니다.
2. **복구 파일 이력 제공 부재**
   * 앱 실행 시 `cleanup_temp_folder()`를 무조건 호출해 temp 폴더를 비우도록 설계되어 있습니다. 비정상 종료 시 사용자가 복구용 temp 파일을 인지하고 활용할 수 있는 복구 정보 이력 화면이 장기적으로 필요할 수 있습니다.
