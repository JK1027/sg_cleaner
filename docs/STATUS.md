# STATUS (현재 개발 상태)

이 문서는 생기부 개인정보 익명화 도구 프로젝트의 **현재 구현 상태**, **다음 작업 계획**, **알려진 이슈** 및 **기술 부채**를 관리합니다.

---

## 1. 현재 구현율 및 진척 상황

* **현재 상태**: **v1.3.0 배포 준비 완료 (GPT 코드 리뷰 피드백 반영 완료)**
* **전체 진척률**: **100%**
* **개발 현황**:
  * 뼈대 구축(1단계) ➡️ 엔진 및 비동기 구현(2단계) ➡️ QSS 디자인 및 오동작 방지 폼 락(3단계) ➡️ 한글 경로 대응 및 PyInstaller 빌드 검증(4단계)을 통과한 뒤, GPT 리뷰 피드백에 따른 안정성 패치를 완료하였습니다.

---

## 2. 다음 작업 계획 (Next Tasks)

* [ ] 실사용자(학교 업무 담당 교사) 환경 배포 및 3열 격자 입력 UI 편의성 피드백 수집.
* [ ] 대용량 엑셀 대장 탐색 및 매핑 연산 성능 모니터링.

---

## 3. 알려진 이슈 (Known Issues)

* **현재 발견된 활성 버그는 없습니다.**
* **탐지 방식의 한계 사항**:
  * 단순 `in` 키워드 기반 매칭 방식을 사용하기 때문에, 결합어가 사용된 단어(예: '김민수' 검색 시 '김민수학생', '김민수부' 등)도 함께 검출됩니다.
  * *대응책*: 사용자가 하단 검수 테이블에서 개별 행의 적용 여부를 수동으로 활성/비활성 처리하여 가려내야 합니다.

---

## 4. 기술 부채 (Technical Debt)

1. **AppState 직접 수정 구조**
   * 현재 `AppController` 내에서 `self.state.selected_files = ...` 형식으로 상태 필드를 직접 덮어쓰고 있습니다. 프로젝트 규모가 확장될 경우 상태 추적이 어려울 수 있으므로, 상태 변경용 액션 패턴이나 캡슐화 메서드를 도입하는 개선이 필요할 수 있습니다.
2. **복구 파일 이력 제공 부재**
   * 앱 실행 시 `cleanup_temp_folder()`를 무조건 호출해 temp 폴더를 비우도록 설계되어 있습니다. 비정상 종료 시 사용자가 복구용 temp 파일을 인지하고 활용할 수 있는 복구 정보 이력 화면이 장기적으로 필요할 수 있습니다.
